import { FC } from 'react';

const CardButton: FC = () => (
  <button className="w-fit bg-blue-500 text-white py-2 px-4 rounded-lg hover:bg-blue-600">
    AÑADIR
  </button>
);

export default CardButton;
