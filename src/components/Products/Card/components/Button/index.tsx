import { FC } from 'react';

const CardButton: FC = () => (
  <button className="w-fit bg-blue-600 text-white py-1 px-3 rounded-lg hover:bg-blue-700">
    AÑADIR
  </button>
);

export default CardButton;
